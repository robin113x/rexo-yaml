apiVersion: v1
kind: Pod
metadata:
  name: pod3
spec:
  containers:
    - name: nginx 
      image: nginx
      imagePullPolicy: IfNotPresent
      volumeMounts:
        - name: myconfig
          mountPath: /etc/config
  volumes:
    - name: myconfig
      configMap:
        name: myconfig
        
