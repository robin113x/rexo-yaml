apiVersion: v1
kind: PersistentVolume
metadata:
    name: jenkins-pv
spec:
    storageClassName: nfs
    capacity:
        storage: 5Gi
    accessModes:
        - ReadWriteMany
    nfs:
        path: /var/nfs/jenkins-data
        server: 192.168.122.131
