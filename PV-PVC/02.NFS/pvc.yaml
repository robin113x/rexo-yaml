apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pvc-example
spec:
    storageClassName: nfs
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 1Gi
