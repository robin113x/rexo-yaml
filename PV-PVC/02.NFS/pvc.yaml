apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: jenkins-home
spec:
    storageClassName: nfs
    accessModes:
        - ReadWriteMany
    resources:
        requests:
            storage: 1Gi
