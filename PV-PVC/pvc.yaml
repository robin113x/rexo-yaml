apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: pvc-example
spec:
    storageClassName: standard-pv
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
