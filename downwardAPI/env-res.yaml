apiVersion: apps/v1
kind: Deployment
metadata:
  name: env-var-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: env-var-app
  template:
    metadata:
      labels:
        app: 
    spec:
      containers:
        - name: env-var-container
          image: busybox
          ports:
            - containerPort: 
