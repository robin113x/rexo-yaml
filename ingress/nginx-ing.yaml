apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-webapp
spec:
  rules:
    - host: 
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx-webapp
                port:
                  number: 80