apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: sd
spec:
  selector:
    matchLabels:
      app: node-daemonset
  template:
    metadata:
      labels:
        app: node-daemonset
    spec:
      containers:
        - name: node-container
          image: nginx
          imagePullPolicy: Always